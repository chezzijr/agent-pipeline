# Test fixture for end-to-end dispatcher runs. Simulates an agent by echoing a
# canned result -- the reply is read from FAKE_RESULT in the environment.
effort_flag = ""
readonly_tools = ""
write_tools = ""
max_usd = 0
cmd = """printf 'result: %s\\nsummary: fake %s agent\\nfiles_declared: [thing.py]\\n' \
"${{FAKE_RESULT:-ok}}" "{model}" > .project/tickets/{id}.result"""
