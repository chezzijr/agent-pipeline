# Test fixture for end-to-end dispatcher runs. Simulates an agent by echoing a
# canned result -- the reply is read from FAKE_RESULT in the environment.
effort_flag = ""
session_flag = ""
readonly_tools = ""
write_tools = ""
max_usd = 0
cmd = """sleep "${{FAKE_SLEEP:-0}}"; printf 'result: %s\\nsummary: fake %s agent\\nfiles_declared: [%s]\\ntest_file: %s\\n' \
"${{FAKE_RESULT:-ok}}" "{model}" "${{FAKE_FILES:-thing.py}}" "${{FAKE_TEST_FILE:-null}}" > {result_file}"""
